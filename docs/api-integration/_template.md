# 对接纪要：\<模块名\>

- **日期：** YYYY-MM-DD
- **范围：** （页面 / 能力，如：管理后台 · 用户管理）
- **相关路径：** `src/app/pages/...`、`src/api/...`
- **OpenAPI：** `src/api/docs/api.json`

## 变更记录

| 日期 | 说明 |
| --- | --- |
| YYYY-MM-DD | 初稿 |

## 已对接（可联调）

| 能力 | 接口 | 前端封装 | 备注 |
| --- | --- | --- | --- |
| 例：用户分页 | `POST /temp/sys-user/page` | `fetchAuthUsers` | `entity.role=USER` |

## 关联接口清单（有表单选项 / 外键时必填）

| 依赖能力 | 接口 | 状态 | 说明 |
| --- | --- | --- | --- |
| 例：预置场景 | `GET /temp/sys-dict/presetScene` | 已接 / 无接口 / 待确认 | 对应 DTO 字段 `presumedSceneDimensionId` |
| 例：维度下拉 | `GET /temp/sys-dict/dimensionDropdown` | | |
| 例：模型下拉 | `GET /temp/depth-model/dropdown` | | |

> 主实体 CRUD 之外，凡 OpenAPI 已有、用于填表单选项的接口，同轮应闭环；详见 api-integration Skill「关联接口闭环」。

## 字段映射

| UI 列 / 能力 | 接口字段 | 状态 | 说明 |
| --- | --- | --- | --- |
| 用户 | `username` | 已确认 | |
| 联系方式 | — | 待确认 | 见下方 Q1 |
| 最后登录 | — | 待确认 | 见下方 Q2 |

状态取值：`已确认` | `待确认` | `已选定：方案X` | `已实现` | `不做`

## 待确认事项（不阻塞其余对接）

### Q1：\<问题标题\>

- **现状：** （如：api.json 的 SysUser 无 email / 无独立停用接口）
- **影响：** （哪个 UI / 哪个操作）
- **方案：**
  1. …
  2. …
  3. …
- **建议（仅建议）：** 方案 _
- **状态：** 待确认

### Q2：…

## 后端缺口（如需同步后端）

| 缺口能力 | 现状 | 建议接口 / 字段 | 前端临时策略 |
| --- | --- | --- | --- |
| 例：停用用户 | 无专用接口 | `PATCH .../status` 或明确可用 update+deleted | 按钮暂不开放 / 等选定 |

## 过时文案清理

| 文案位置 | 处理 | 说明 |
| --- | --- | --- |
| 例：AdminLogin 联调提示 | 已删除 | 登录已接真实鉴权 |
| 例：任务推送「待后端通知」 | 保留 | 通知接口未接 |

## 验收要点

- [ ] Network 可见已对接路径
- [ ] 样式未改布局
- [ ] 待确认项未擅自拼接
- [ ] 刷新 / 分页 / 角色（如适用）正常
- [ ] 过时联调文案已按上表处理
